<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>
<div><%= link_to "Edit task", edit_path %></div>
<div><%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
</div>
<div><%= link_to "Back to list", index_path %></div>
